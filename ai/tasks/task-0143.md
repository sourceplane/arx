# Task 0143

Agent: Implementer

## Current Repo Context

- Task 0141.1 verified PASS and merged PR #141. GitHub Artifacts Requirement 11 is complete: `orun github runs --details` downloads manifest-only data and prints Level 2 shard detail.
- Task 0142 was a verifier pass over PR #142 (`happy-patch-113`). It produced `ai/reports/task-0142-verifier.md` with Result: FAIL. PR #142 remains OPEN and must not merge as-is.
- PR #142 contains one useful GitHub CLI UX fix (`ddbec4c`) plus blockers: queued/unstable CI, a dummy `trigger: pr-142-dummy-change`, unrelated TUI cockpit/process/history files, a meaningless PR title/body, and no implementer report.

## Objective

Repair PR #142 into a coherent, mergeable GitHub CLI UX fix PR: keep only the task-scoped CLI/docs changes for `--orun-dir` normalization and `orun github status` resolver flags, remove unrelated scope and dummy CI trigger content, retitle/rewrite the PR, and provide a proper implementer report so verification can rerun.

This satisfies the GitHub Artifacts stabilization roadmap by preserving the valid follow-up from Task 0142 while protecting `main` from unrelated spec/process/history changes and temporary CI-trigger artifacts.

## PR Boundary

This task reuses PR #142 if possible; opening a successor PR is allowed only if GitHub branch repair is impractical.

In scope:

1. Keep or recreate the CLI behavior from PR #142 commit `ddbec4c`:
   - `cmd/orun/command_github.go` normalizes `orun github pull --orun-dir` so a working directory path resolves to its `.orun/` child, while paths already ending in `.orun` remain unchanged for compatibility.
   - `orun github status` registers the same run-resolution flags as `pull`/`logs`: `--run-id`, `--exec-id`, `--sha`, `--branch`, `--latest`, and `--failed`.
2. Keep task-scoped documentation only:
   - `website/docs/cli/orun-github.md` documents the revised `--orun-dir` semantics, status flags, resolution order, and caveats that correspond to the code change.
   - `docs/github-log-pull-ux-review.md` may remain only if it directly explains the UX walkthrough that motivated this CLI/doc fix.
3. Remove blockers from PR #142:
   - Revert `examples/apps/api-edge/component.yaml` dummy label `trigger: pr-142-dummy-change`.
   - Remove `.kiro/specs/orun-tui-cockpit/**`, `orun-tui-cockpit.md`, `agents/orchestrator.md`, historical `ai/tasks/task-0139-verifier.md`, `ai/tasks/task-0140.md`, `ai/tasks/task-0140-verifier.md`, `ai/tasks/task-0141-verifier.md`, and stale `ai/waiting_for_input.md` from this PR branch unless they already exist on `main` independently.
4. Update PR metadata and report:
   - Retitle to a meaningful subject such as `fix(github): normalize --orun-dir and register status flags`.
   - Rewrite the PR body with scope, tests, and verification notes.
   - Add `ai/reports/task-0143-implementer.md` on the PR branch.

## Read First

- `ai/reports/task-0142-verifier.md` — exact blockers and acceptable code/docs from the failed verification.
- `ai/tasks/task-0142-verifier.md` — original verifier acceptance criteria for PR #142.
- `.kiro/specs/github-artifacts/requirements.md` — Requirements 12, 13, 15, and 20 context for GitHub CLI behavior and tests.
- `website/docs/cli/orun-github.md` — public CLI docs that must match the implementation.
- `cmd/orun/command_github.go` and `cmd/orun/command_github_test.go` — CLI implementation and focused tests.
- PR #142 diff and commits:
  - `gh pr view 142 --json number,title,body,headRefName,baseRefName,state,isDraft,mergeStateStatus,statusCheckRollup,commits,files,url`
  - `gh pr diff 142 --name-only`

## Required Outcomes

- [ ] PR #142 branch is narrowed to CLI/doc scope only, or a successor PR is opened with the same narrow scope and PR #142 is clearly superseded/closed.
- [ ] `examples/apps/api-edge/component.yaml` no longer contains `trigger: pr-142-dummy-change` in the PR diff.
- [ ] TUI cockpit spec files, root `orun-tui-cockpit.md`, `agents/orchestrator.md`, historical task prompts, and stale `ai/waiting_for_input.md` are not part of this PR diff.
- [ ] The PR title/body accurately describe the GitHub CLI UX fix and testing performed.
- [ ] `ai/reports/task-0143-implementer.md` is committed to the PR branch with the real PR number.
- [ ] Focused tests/build pass locally before pushing.
- [ ] Required GitHub checks are re-triggered and `Orun Plan` is no longer queued/unknown before reporting complete, unless GitHub infrastructure prevents that after a re-run attempt and the report documents the blocker.

## Non-Goals

- Do not implement TUI cockpit Phase 1.
- Do not land `.kiro/specs/orun-tui-cockpit/**`, `orun-tui-cockpit.md`, or `agents/orchestrator.md` in this PR.
- Do not add historical orchestration task prompts or unrelated state-file cleanup to this PR.
- Do not implement new GitHub CLI features beyond `--orun-dir` normalization, `github status` resolver flags, and matching docs/tests.
- Do not alter artifact upload/download schemas, workflow templates, or E2E CI workflow coverage.
- Do not merge PR #142 yourself; verification and merge are the Verifier's responsibility.

## Constraints

1. Preserve a coherent one-PR boundary: GitHub CLI UX fix plus directly matching docs/tests only.
2. If reusing PR #142, force-pushing is acceptable only after confirming the intended final branch contents. Do not lose the useful `ddbec4c` code/doc changes.
3. The dummy `trigger: pr-142-dummy-change` must not land on `main`.
4. Keep TUI cockpit specs and `agents/orchestrator.md` available for later dedicated PRs if they are useful, but do not include them here.
5. Do not print or commit credentials, GitHub tokens, signed artifact URLs, or raw secret-bearing logs.
6. All task-scoped changes must be committed, pushed, and attached to a real GitHub PR before reporting complete.

## Acceptance Criteria

✅ Final PR diff is narrow and reviewable:
```bash
gh pr diff 142 --name-only
```
Expected files are limited to task-scoped files such as:
- `cmd/orun/command_github.go`
- `cmd/orun/command_github_test.go` if tests are added/adjusted
- `website/docs/cli/orun-github.md`
- `docs/github-log-pull-ux-review.md` if retained as direct UX-review context
- `ai/reports/task-0143-implementer.md`

✅ The dummy trigger is absent:
```bash
gh pr diff 142 -- examples/apps/api-edge/component.yaml
```
Expected: no diff, or no `trigger: pr-142-dummy-change` anywhere.

✅ Unrelated scope is absent:
```bash
gh pr diff 142 --name-only | grep -E '^(\.kiro/specs/orun-tui-cockpit/|orun-tui-cockpit\.md|agents/orchestrator\.md|ai/tasks/task-0139-verifier\.md|ai/tasks/task-0140\.md|ai/tasks/task-0140-verifier\.md|ai/tasks/task-0141-verifier\.md|ai/waiting_for_input\.md)$' && exit 1 || true
```
Expected: command exits successfully with no matched blocker files.

✅ Local tests/build pass:
```bash
go test ./cmd/orun/ -run 'TestGithub(Status|Pull|Logs|Runs)|TestGithubCommand' -v
go test ./internal/artifactstore/github/... -v
go test ./internal/runbundle/... -v
go test ./cmd/orun/... -v
go build ./cmd/orun/
```

✅ PR metadata is meaningful:
```bash
gh pr view 142 --json title,body,mergeStateStatus,statusCheckRollup
```
Expected: title/body describe the CLI UX fix; no `chore: update happy-patch-113` placeholder.

✅ GitHub checks are re-run and no required check is queued, failing, cancelled, or unknown when you report complete. If `Orun Plan` remains stuck after a re-run attempt, report that blocker explicitly and do not claim the task is complete.

## Verification

The Verifier will:

1. Re-read `ai/reports/task-0142-verifier.md` and this task prompt.
2. Inspect the final PR diff and commits for narrow scope.
3. Confirm code behavior for `--orun-dir` normalization and `github status` flags.
4. Run the focused Go tests/build listed above.
5. Inspect PR CI logs and required check rollup; merge only if all required checks pass.
6. Confirm no unrelated TUI cockpit/process/history/dummy-trigger files remain in the PR.
7. Confirm `ai/reports/task-0143-implementer.md` exists and names the real PR number.

## PR Creation Requirement

You must reuse PR #142 after narrowing it, or create a successor PR if PR #142 cannot be safely repaired. In either case, before reporting complete you must have a real GitHub PR number, committed task-scoped changes, pushed branch, meaningful PR title/body, and an implementer report with the real PR number. `PR Number: TBD` is not acceptable.

## When Done Report

Write `/ai/reports/task-0143-implementer.md` with:

- Summary
- PR Number and URL
- Branch / base
- Files Changed
- Scope Removed From PR #142
- Checks Run with exact command output summaries
- CI Status / Re-run Evidence
- Assumptions
- Risks / Follow-ups
