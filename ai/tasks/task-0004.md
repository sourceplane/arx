# Task 0004

Agent: Implementer

## Current Repo Context

- Active spec: `specs/orun-state-redesign/` (Phase 1, local-only, trigger-first
  revision-first state model). Authoritative.
- Active milestone: **M2 — `internal/statestore` (local driver)**.
- Last completed: Task 0003 (M2 PR A) — frozen `StateStore` interface, four-error
  taxonomy, `paths.go` helpers, and `LocalStore` non-CAS subset
  (`Root`, `Read`, `Write`, `CreateIfAbsent`, `Delete`) merged via PR **#154**
  to main commit `9b0a39c` on 2026-05-29. Coverage on `internal/statestore`
  measured 95.4 % locally.
- `*LocalStore.CompareAndSwap` and `*LocalStore.List` are stubbed in
  `internal/statestore/local.go` (lines ~208–219), each returning
  `fmt.Errorf("%w: … not implemented in PR A (M2 PR B)", ErrInvalid)`.
  Higher layers MUST NOT call them yet; this task delivers the real
  implementations.
- No production callers of `internal/statestore` exist yet (`cmd/orun`,
  `internal/state`, `internal/runner`, `internal/runbundle` are untouched).
  PR B keeps that property — it is still a leaf-clean package.
- Repo health 🟢 green. Branch: `main` synced with `origin/main`. No open PRs
  in the state-redesign lineage.

## Objective

Replace the PR-A stubs with the real Phase-1 local implementations of
`*LocalStore.CompareAndSwap` and `*LocalStore.List`, and add the M2 atomicity /
exclusivity / property-based test suite required by `test-plan.md` §2 and §3.
This closes the M2 contract on the local driver and unblocks PR C
(typed refs/indexes marshallers) plus M3 (`internal/revision`).

## PR Boundary

In scope (one PR on branch `impl/task-0004-m2-statestore-prb`):

1. Real `*LocalStore.CompareAndSwap` per `state-store.md` §3.3 — `Read` →
   compare `meta.Revision` to `oldRev` → `Write`. Returns errors wrapping
   `ErrNotFound` on absent target and `ErrConflict` on revision mismatch. The
   small Phase-1 race between `Read` and `Write` is acceptable and explicitly
   documented in §3.3; do NOT introduce cross-process locking. A per-path
   in-process mutex (e.g. `sync.Map[string]*sync.Mutex`) is acceptable as long
   as the contract still says "best-effort on local; native on remote (future)"
   per `state-store.md` §6 and the property test for two-CAS-one-wins still
   admits the documented intra-process serialization.
2. Real `*LocalStore.List` per `state-store.md` §3.4 — walks the translated
   directory tree rooted at the translated prefix, returns `[]ObjectInfo`
   in unspecified order, skips symlinks (Phase 1 layout introduces none),
   ignores tempfiles matching `.orun-tmp-*`, returns logical (forward-slash)
   paths.
3. Atomicity / exclusivity / property test suite per `test-plan.md` §2 and §3.1
   shape (adapted to the statestore layer):
   - 100-goroutine `Write` + concurrent `Read` atomicity — readers always
     decode complete JSON; zero decode errors over the run.
   - 100-goroutine `CreateIfAbsent` exclusivity on the same path — exactly one
     wins (`nil`), the rest return errors wrapping `ErrExists`.
   - Concurrent CAS conflict — two goroutines `CompareAndSwap` with the same
     `oldRev`; exactly one returns nil, the other returns an error wrapping
     `ErrConflict`. Use a small loop (e.g. 200 iterations) to make the race
     deterministic enough for CI.
   - `pgregory.net/rapid` round-trip — arbitrary path-alphabet inputs
     (segments matching `[a-zA-Z0-9._-]{1,32}`, depth 1–5, ASCII bytes for
     payload) survive `Write` → `Read` byte-for-byte and produce stable
     `ObjectMeta.Revision` (lowercase-hex sha256 already implemented in
     `hashRevision`). `pgregory.net/rapid v1.1.0` is already pinned in
     `go.mod`.
4. Coverage gate on `internal/statestore` stays ≥ 95 %; the goal is ≥ 96 %
   once stubs are gone. Update `make test-state-redesign` only if the
   threshold check or package list needs adjustment (it should not — the
   target already runs `./internal/statestore/...` against ≥ 95 %).

Out of scope (do NOT include — these belong to other tasks):

- No `refs.go` / `indexes.go` / typed marshallers (PR C).
- No production-caller wiring (`cmd/orun`, `internal/state`,
  `internal/runner`, `internal/runbundle` stay untouched).
- No new exported symbols beyond what `state-store.md` §1 already names.
- No spec changes. If a behavioral question arises that requires a spec
  decision (e.g. List ordering, symlink policy beyond §3.4), file a proposal
  under `ai/proposals/task-0004-spec-update.md` and proceed conservatively.
- No empty-directory `Delete` semantics revisit (verifier left a non-blocking
  Minor on this in Task 0003; don't change it here).
- No cross-process file locks. The race in §3.3 is allowed.

## Read First

- `specs/orun-state-redesign/README.md` (entry + read order; status table).
- `specs/orun-state-redesign/implementation-plan.md` — Milestone M2 ("Suggested
  PR scope" → PR B; "Done when" criteria, lines ~95–106).
- `specs/orun-state-redesign/state-store.md` — §1 (interface), §3.3
  (CompareAndSwap), §3.4 (Read / List / Delete), §4 (error taxonomy), §6
  (atomicity contract summary).
- `specs/orun-state-redesign/test-plan.md` — §1 (coverage targets, ≥ 95 % on
  `internal/statestore`), §2 (atomicity suite), §3 (property tests).
- `internal/statestore/local.go` (current PR-A implementation) and
  `internal/statestore/local_test.go` (existing patterns to mirror).
- `internal/statestore/paths.go` + `paths_test.go` (alphabet / validation).
- `Makefile` target `test-state-redesign` (lines 101–109).
- Reference only: `specs/orun-state-redesign/design.md` §7 (correctness
  properties) for context.

## Required Outcomes

- [ ] `*LocalStore.CompareAndSwap` is a real implementation (the PR-A stub is
      gone) and matches `state-store.md` §3.3.
- [ ] `*LocalStore.List` is a real implementation (the PR-A stub is gone) and
      matches `state-store.md` §3.4.
- [ ] All four atomicity / exclusivity / CAS / property tests live in
      `internal/statestore/*_test.go` and pass under `-race`.
- [ ] `go build ./...` clean.
- [ ] `go vet ./...` clean.
- [ ] `go test -race -count=1 ./internal/statestore/...` green.
- [ ] `make test-state-redesign` green; reported coverage on
      `internal/statestore` ≥ 95 % (target ≥ 96 %).
- [ ] No new dependencies beyond what is already in `go.mod`.
- [ ] `internal/statestore` remains leaf-clean (`go list -deps
      ./internal/statestore/...` shows zero `…/orun/internal/*` imports
      besides itself; `pgregory.net/rapid` in test files is fine).
- [ ] Implementer report at `/ai/reports/task-0004-implementer.md` filed
      with the budget shape from `agents/orchestrator.md`.
- [ ] Branch `impl/task-0004-m2-statestore-prb` pushed; PR opened against
      `main`; PR Number recorded in the report (no `TBD`).

## Non-Goals

- No typed refs/indexes helpers (those are PR C / Task 0005).
- No production-caller migration (`internal/state`, `cmd/orun`, runner,
  runbundle).
- No empty-directory Delete change.
- No remote driver work; Phase 1 is local-only.
- No CLI surface changes.
- No documentation refactor beyond godoc comments on the two real methods.

## Constraints

1. Match the frozen interface in `internal/statestore/store.go` exactly. Any
   signature drift is a contract violation and requires a proposal under
   `ai/proposals/`.
2. All errors returned by the new code MUST wrap one of the four sentinels
   (`ErrNotFound`, `ErrExists`, `ErrConflict`, `ErrInvalid`) via
   `fmt.Errorf("%w: …", ErrX, …)`. No string sniffing. Detect via
   `errors.Is` / `errors.As` only.
3. `List` MUST return logical paths (forward-slash, root-relative, no leading
   slash) and MUST NOT include `.orun-tmp-*` orphan tempfiles.
4. CAS implementation MUST tolerate the documented race (§3.3); no
   cross-process locking, no `flock`, no fsync-loop heroics.
5. Property tests MUST use `pgregory.net/rapid` v1.1.0 (already pinned). Do
   NOT add `gopter` or any other property-based testing library.
6. Tests MUST run under `-race` without flake. Goroutine-count knobs (e.g.
   `N=100`) are acceptable but the test should not introduce arbitrary
   `time.Sleep` for synchronization — use channels / `sync.WaitGroup`.
7. No new exported types or functions beyond what the interface already
   demands. If a helper is needed, keep it lowercase.
8. `internal/statestore` stays leaf-clean (no `internal/*` imports).

## Integration Notes

- The atomicity test should reuse the workspace harness from
  `internal/testfx/statefs` if it fits; otherwise local `t.TempDir()` is fine.
- `decodeStrict` in the spec's pseudocode (§2 of `test-plan.md`) maps to
  `json.NewDecoder(bytes.NewReader(b)).DisallowUnknownFields()` followed by
  `Decode`. Pick one shape and document it in a comment.
- The `rapid` round-trip test should generate paths via
  `rapid.SliceOfN(rapid.StringMatching(\`[a-zA-Z0-9._-]{1,32}\`), 1, 5)` and
  join with `/`. Use `rapid.SliceOfBytesMatching` or
  `rapid.SliceOfN(rapid.Byte(), 0, 4096)` for payloads.
- `LocalConfig.Clock` is already an additive test-injection hook; you may use
  it for any deterministic sweep timing in tests but don't bake real time
  dependencies into production code.
- The persistent local-only `kiox -- orun plan --changed --intent
  examples/intent.yaml` failure on composition-cache resolution
  (documented in `ai/state.json` notes) is NOT a regression; CI is
  authoritative. Run `kiox -- orun validate --intent intent.yaml` and the
  full plan/run dry-run sequence per the Implementer Standard, but if the
  `--changed` invocation reproduces the known cache failure on your
  machine, record the exact output verbatim in the report under
  Assumptions and proceed. CI must still be green.

## Acceptance Criteria

✅ Local quality gates pass:
```
go build ./...
go vet ./...
go test -race -count=1 ./internal/statestore/...
make test-state-redesign
```

✅ `make test-state-redesign` reports `internal/statestore` coverage ≥ 95 %
(target ≥ 96 %); the gate's `awk` check exits 0.

✅ Orun verification (per Implementer Standard) recorded in the report:
```
/Users/irinelinson/.local/bin/kiox -- orun validate --intent intent.yaml
/Users/irinelinson/.local/bin/kiox -- orun plan --changed --intent intent.yaml --output plan.json
/Users/irinelinson/.local/bin/kiox -- orun run --plan plan.json --dry-run --runner github-actions
```
Record the exact stderr/stdout if the known composition-cache resolution
failure reproduces locally; CI of the PR must still show real `orun plan`
and dry-run guard runs green.

✅ The PR-A stub error strings (`"… not implemented in PR A (M2 PR B)"`) no
longer appear in `internal/statestore/local.go`.

✅ `git grep -n "internal/statestore" -- internal/state cmd/orun
internal/runner internal/runbundle` shows no new imports.

✅ Branch `impl/task-0004-m2-statestore-prb` pushed; PR opened against
`main`; PR description summarizes scope, links the spec sections, and
includes the local quality-gate output. PR CI green on all required checks.

✅ Implementer report `/ai/reports/task-0004-implementer.md` exists, follows
the budget in `agents/orchestrator.md`, and the `PR Number` line is the
real PR number (not `TBD`, not `BLOCKED` unless legitimately blocked).

## Verification

The Verifier will:

- Diff the PR against `state-store.md` §3.3 and §3.4 and confirm semantics
  (error wrapping, list ordering / tempfile filtering, CAS race tolerance).
- Re-run `make test-state-redesign` and `go test -race ./internal/statestore/...`
  locally and confirm coverage ≥ 95 %.
- Read PR CI logs (not just status badges) for `CI / Orun Plan` and the
  harness dry-run guard, confirming real invocations.
- Confirm `internal/statestore` is still leaf-clean (no new `internal/*`
  imports) and no production-caller wiring slipped in.
- Apply the Verifier Merge Protocol from `agents/orchestrator.md` (sections
  on Verifier Standard) — merge on PASS, fast-forward main, delete branch,
  leave repo clean.

## PR Creation Requirement

You must create branch `impl/task-0004-m2-statestore-prb`, commit the
implementation + tests, push, and open a GitHub PR before reporting
complete. `PR Number: TBD` is not an acceptable terminal state. After
creating the PR, update the implementer report with the real PR number,
commit, and push that final update. If PR creation is blocked (e.g. `gh`
auth failure), report `PR Number: BLOCKED` with the exact command and
error and stop — do NOT mark the task complete.

## When Done Report

Write `/ai/reports/task-0004-implementer.md` with:

- Summary (3–5 bullets)
- Files Changed (grouped by subsystem)
- Checks Run (exact commands + result)
- Assumptions (only durable assumptions; include the `--changed` cache
  reproduction note if applicable)
- Spec Proposals (links only, with one-line reason; expected: none)
- Remaining Gaps (residual risk for PR C / M3, if any)
- Next Task Dependencies (what M2 PR C / M3 unlocks once this lands)
- PR Number (real GitHub PR number, no `TBD`)
