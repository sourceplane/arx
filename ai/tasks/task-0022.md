# Task 0022

## Agent
Implementer

## Current Repo Context
- Phase 1 (`specs/orun-state-redesign/`, M0–M6) is structurally complete.
  PRs #152–#165 are merged; release-notes PR #166 (`b4178dd`) closed it out.
  Coverage floors on `main`: `internal/statestore` 95.7 %,
  `internal/revision` 90.3 %, `internal/executionstate` 90.0 %.
- `ai/state.json` has been rotated to Phase 2 (`active_spec =
  specs/orun-component-catalog`, `active_milestone = C0`,
  `phase_history.phase_1_orun_state_redesign` recorded). The Phase 1 coverage
  floors are pinned there as preserved invariants and MUST NOT be lowered.
- The working tree currently carries the un-committed Phase 2 rollover
  artifacts that this PR ships:
  - `specs/orun-component-catalog/` — 12-doc engineering design pack
    (~2 945 lines), authoritative for Phase 2.
  - `orun-catalog-full-design.md` — 2 090-line monolithic working draft
    from which the 12 docs were distilled. **This file does not belong
    at the repo root long-term.**
  - `agents/orchestrator.md` — rewritten Active-Architecture-Source block,
    operational-access section, embedded `state.json` template, and
    example task prompt to point at the new spec pack and milestones C0–C9.
  - `ai/state.json` — rotated as described above.
- `ai/context/current.md` and `ai/context/task-ledger.md` still describe
  the M6 closure as the "current" frame. They must be updated in this PR
  to reflect the Phase 1 → Phase 2 rollover; the task-ledger ends at
  Task 0021 and must gain a Task 0022 entry.

## Objective
Land the Phase 2 rollover as a single, reviewable, **docs-and-bookkeeping
only** PR. After this PR merges, the repo on `main` carries the
authoritative `specs/orun-component-catalog/` spec pack, the orchestrator
prompt that points at it, the rotated state files, and a clean ledger —
unblocking the C0 code half (Task 0023) and every later milestone.

## PR Boundary
In scope:
1. Stage and commit `specs/orun-component-catalog/` exactly as it sits
   in the working tree (no content rewrites in this task; spec authoring
   is done — fix only obvious cross-file link rot or front-matter typos
   discovered while reading).
2. Stage and commit `agents/orchestrator.md` and `ai/state.json` (already
   modified locally) as the Phase 2 rollover.
3. Decide and execute the placement of `orun-catalog-full-design.md`:
   - Preferred: `git rm orun-catalog-full-design.md` because its content
     has been distributed across the 12 spec docs; OR
   - If you find content in the monolith that is NOT yet covered by any
     of the 12 docs, move it to
     `specs/orun-component-catalog/_archive/full-design-monolith.md`
     and add a one-line `_archive/README.md` explaining it is a
     historical draft kept for provenance only and is NOT authoritative.
   - Either way, the repo root must not retain the monolith.
4. Refresh `ai/context/current.md` to describe the Phase 2 frame:
   active spec, active milestone (C0), what the next implementer task
   (Task 0023) will produce, and the Phase 1 closure summary moved into
   a "Past Phase" section.
5. Append a `## Task 0022` entry to `ai/context/task-ledger.md` (and a
   short "Phase 2 begins" header above it) following the established
   ledger shape.
6. Refresh `ai/waiting_for_input.md` to a short "no input currently
   requested" note — Phase 2 has no blockers right now.

Out of scope (becomes Task 0023+ or later):
- Any Go code: `internal/catalogmodel`, `internal/sourcectx`, JSON-Schema
  generator, `go generate`, `make test-state-redesign` extensions.
- Any CLI surface change (`orun catalog *`, new flags).
- Any writes under `.orun/sources/` or `.orun/catalogs/`.
- Any spec content rewrites beyond mechanical fixes (typos, broken
  intra-spec links). Substantive spec changes go through
  `/ai/proposals/`.
- Any change to existing Phase 1 packages (`internal/statestore`,
  `internal/triggerctx`, `internal/revision`, `internal/executionstate`).

## Read First
- `specs/orun-component-catalog/README.md` — entry point, doc index,
  read order, and how each agent role uses the pack.
- `specs/orun-component-catalog/implementation-plan.md` — Milestone C0
  goal, dependencies, suggested PR scope, "done when". (Note: C0's
  code half ships in Task 0023; this task ships only the spec-landing
  half.)
- `agents/orchestrator.md` — the rewritten Phase 2 sections you are
  committing; cross-check that every reference to `specs/orun-state-redesign/`
  is correctly demoted to "predecessor (Phase 1, COMPLETE — M0–M6
  merged)" and every active-spec reference points at the new pack.
- `ai/state.json` — confirm the rotated values match the rollover
  shape (active_spec, active_milestone=C0, phase_history block,
  task_agent updated to this prompt).
- `ai/context/current.md` and `ai/context/task-ledger.md` — for the
  context-file refresh.

Reference only:
- `specs/orun-state-redesign/README.md` — confirm Phase 1 is preserved
  intact; do NOT edit Phase 1 specs in this PR.

## Required Outcomes
- [ ] `specs/orun-component-catalog/` (12 .md files) committed.
- [ ] `agents/orchestrator.md` Phase 2 rewrite committed.
- [ ] `ai/state.json` Phase 2 rotation committed; `task_agent` points
      to `ai/tasks/task-0022.md` at the time of the implementer-report
      commit, then to `ai/tasks/task-0022-verifier.md` after the
      verifier prompt is added (verifier task may be folded into a
      single-pass closure per the established convention; in that
      case, leave `task_agent` pointing at the implementer prompt
      until verifier work begins).
- [ ] `orun-catalog-full-design.md` removed from the repo root
      (deleted OR archived under `specs/orun-component-catalog/_archive/`
      with a README; document the choice in the implementer report).
- [ ] `ai/context/current.md` rewritten for the Phase 2 frame.
- [ ] `ai/context/task-ledger.md` gains a `## Task 0022` entry; the
      Phase 1 ledger above it is preserved verbatim.
- [ ] `ai/waiting_for_input.md` reset to a "no input" note.
- [ ] Branch created (suggested: `impl/task-0022-phase2-rollover`),
      commits pushed, GitHub PR opened.
- [ ] Implementer report written at
      `ai/reports/task-0022-implementer.md`.

## Non-Goals
- No production Go changes.
- No CLI changes.
- No new tests, no coverage shifts.
- No edits to Phase 1 specs.
- No HTTP, no SaaS, no DB schema work (Phase 2 is local-only;
  `internal/catalogsync` will ship only `Syncer` interface +
  `NoopSyncer` in C9 — far in the future).

## Constraints
1. The 12-doc spec pack ships as-is. If you find broken intra-spec
   links, frontmatter typos, or stale "TODO" markers obviously left
   behind by spec extraction, fix them in this PR; otherwise file an
   `/ai/proposals/task-0022-spec-update.md` and continue.
2. **Do not rewrite or restructure any spec doc** — substantive content
   changes are spec proposals, not rollover work.
3. Phase 1 invariants preserved: do not rename a Phase 1 field, do
   not lower a Phase 1 coverage floor, do not remove a preserved
   Phase 1 CLI workflow. (Trivially honoured in a docs-only PR; cross-
   check anyway when reviewing the orchestrator.md rewrite.)
4. `agents/orchestrator.md` must keep the Deferred Decision Protocol
   wording the user requires (see the orun-saas-orchestration skill,
   "Anti-Patterns" → "Conflating deferred with blocked"). If the
   rewrite weakened that wording, restore it before committing.
5. PR must be reviewable end-to-end. If the diff exceeds ~3 200 LOC
   (the spec pack alone is ~2 945; orchestrator.md edits add a few
   hundred), keep the commit history clean: one commit for the spec
   pack, one for orchestrator.md + state.json, one for context-file
   refresh + monolith placement, one for the implementer report.
6. No `delegate_task`. Run inline.

## Integration Notes
- Task 0023 (next) consumes `specs/orun-component-catalog/data-model.md`,
  `identity-and-keys.md`, and `implementation-plan.md` Milestone C0
  to ship `internal/catalogmodel` + `internal/sourcectx` skeleton +
  JSON-Schema generator. The rollover must therefore leave those three
  docs accessible at their final paths before Task 0023 is generated.
- The orchestrator's "Active architecture source" block in
  `agents/orchestrator.md` is the contract every future task prompt's
  `Read First` quotes — confirm milestone IDs (C0–C9) and doc names
  match the actual `implementation-plan.md` exactly.

## Acceptance Criteria
- ✅ `git status` is clean after the final commit on the task branch
  (all rollover artifacts committed, no stray untracked files).
- ✅ `git log --oneline origin/main..HEAD` shows a clean, scoped
  commit history matching the constraint above.
- ✅ Repo root no longer contains `orun-catalog-full-design.md`.
- ✅ `specs/orun-component-catalog/{README,implementation-plan,data-model,
  identity-and-keys,resolution-pipeline,catalog-store,
  compatibility-and-migration,cli-surface,sync-model,design,
  test-plan,risks-and-open-questions}.md` all exist on the branch.
- ✅ `agents/orchestrator.md` cites `specs/orun-component-catalog/` as
  the active spec and milestones C0–C9 as the active sequence; the
  predecessor block points at `specs/orun-state-redesign/` marked
  COMPLETE M0–M6.
- ✅ `ai/state.json` parses; `active_spec`, `active_milestone`,
  `phase_history.phase_1_orun_state_redesign`, and `task_agent` are
  set as described above; `waiting_for_input == "false"`.
- ✅ `ai/context/current.md` describes Phase 2 / C0 as the active
  frame; the Phase 1 closure is preserved as a "Past Phase" section.
- ✅ `ai/context/task-ledger.md` ends with a `## Task 0022` entry
  describing this PR; all prior entries (Tasks 0001–0021) are
  preserved verbatim.
- ✅ `go build ./...` is green on the branch (sanity-only — this PR
  changes no Go code).
- ✅ `go test ./... -count=1 -timeout 180s` is green on the branch.
- ✅ `kiox -- orun validate --intent intent.yaml` is green if
  `intent.yaml` exists at repo root.
- ✅ `kiox -- orun plan --changed --intent intent.yaml --output plan.json`
  is green when applicable; record the no-op plan when no jobs are
  scheduled (docs-only PR — expected).
- ✅ `kiox -- orun run --plan plan.json --dry-run --runner github-actions`
  records no-op result when plan has no jobs.
- ✅ PR opened with a description that links to `specs/orun-component-catalog/README.md`
  and explicitly calls out "Phase 2 rollover — docs/bookkeeping only".
- ✅ Required CI checks PASS on the PR head (`Orun Plan`,
  `Harness dry-run guard`); matrix legs SKIPPED legitimately is fine.

## Verification
The verifier (Task 0022 verifier pass) will:
- Re-run the four local commands above on the PR branch.
- Audit the diff for any code drift (must be docs-only + state files).
- Confirm `agents/orchestrator.md` Phase 2 wording matches the
  Deferred Decision Protocol contract (orun-saas-orchestration skill
  Anti-Patterns).
- Confirm Phase 1 specs and Phase 1 coverage floors are untouched.
- Confirm `phase_history.phase_1_orun_state_redesign.coverage_floors`
  match the values on `main` immediately before the rollover.
- Inspect required CI logs on the PR head, not just status summaries.
- If PASS, merge the PR, sync local `main` to `origin/main`, leave the
  worktree clean.

## PR Creation Requirement
You must create a branch (suggested
`impl/task-0022-phase2-rollover`), commit, push, and open a GitHub
PR before reporting complete. `PR Number: TBD` is not acceptable. If
the implementer report and any context files reference a placeholder
PR number, update them with the real number after `gh pr create` and
push that final commit before declaring done.

## When Done Report
File at `ai/reports/task-0022-implementer.md`. Sections:

- Summary (3–5 bullets — what shipped, monolith placement decision,
  PR number)
- Files Changed (grouped: spec pack, orchestrator.md, state files,
  context files, monolith disposition)
- Checks Run (exact commands and results)
- Assumptions (only durable assumptions — e.g. monolith disposition
  rationale)
- Spec Proposals (if any cross-link or typo fix was meaningful enough
  to warrant a `/ai/proposals/task-0022-spec-update.md`, link it)
- Remaining Gaps (residual rollover risk, e.g. any spec doc that
  references a Phase 1 invariant or a yet-to-exist package and should
  be revisited in Task 0023+)
- Next Task Dependencies (what Task 0023 needs from this rollover)
- PR Number (the real `gh pr create` number, not TBD)
